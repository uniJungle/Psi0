## PICO 手柄键位

1. **进入遥操作**：之前的流程 → **A+B+X+Y** 校准姿态 → **A+X**

2. **模式切换**：
    - **A+X**：遥操（POSE）↔ 规划（PLANNER）
    - **Y**：遥操暂停 ↔ 恢复遥操
    - **A+B+X+Y**：急停并退出策略

3. **规划模式移动**：
    - 默认 **SLOW_WALK 模式**：左摇杆控制平移，右摇杆控制朝向

4. **双手开合**：
    - **左 trigger**：左手开合
    - **右 trigger**：右手开合
    
5. **录制 episode**：
    - **left gripper + A**：按一次开始，再按一次停止
    - **left gripper + B**：丢弃当前数据（需要在开始录制触发后生效）
---
**注：不要在录制过程中反复点击上述的键位组合。**

## 代码指令

### 终端 1：

```bash
# 启动 G1 机载图像服务与 Brainco 手部服务
ssh unitree@192.168.123.164

bash ./sonic_start_teleop.sh
```

### 终端 2：

```bash
cd ~/ycb_ws/Psi0/

bash ./real/SONIC/scripts/collect_psi0-sonic-data-manual.sh deploy
```

### 终端 3：

```bash
cd ~/ycb_ws/Psi0/

bash ./real/SONIC/scripts/collect_psi0-sonic-data-manual.sh pico \
    --dds-interface enp4s0 \
    --eef brainco
```

### 终端 4：

```bash
cd ~/ycb_ws/Psi0/

bash ./real/SONIC/scripts/collect_psi0-sonic-data-manual.sh exporter \
    --root-output-dir /home/karthus_chen/ycb_ws/datasets/SONIC \
    --task-prompt "Pick bottle and pour into cup." \
    --task-name "test" \
    --use-stereo-camera \
    --eef brainco
```；再次按Y暂停后，deploy端打印为